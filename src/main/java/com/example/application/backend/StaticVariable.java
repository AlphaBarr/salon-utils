package com.example.application.backend;

public enum StaticVariable {;

    public enum SIZE {
        SMALL_SIZE, MEDIUM_SIZE, LARGE_SIZE
    };
    public enum LENGTH {
        SHOULDER_LENGTH,MID_BACK_LENGTH, LOWER_BACK_LENGTH, WAIST_LENGTH, BUTT_LENGTH
    };

}
